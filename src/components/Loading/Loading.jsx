import React from 'react';
import Logo from '../../assets/Logo';
import './loading.scss';

const Loading = () => {
    return (
        <div className="fade">
            <Logo/>
        </div>
    );
}

export default Loading;
